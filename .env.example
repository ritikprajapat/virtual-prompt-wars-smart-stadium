GEMINI_API_KEY=
RATE_LIMIT_PER_MINUTE=20
# When true, AI phrasing failures degrade to a static message instead of a 502,
# keeping features usable during a Gemini outage. Off by default.
LLM_OFFLINE_FALLBACK=false
